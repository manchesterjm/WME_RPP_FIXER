# Backup files
*.zip

# Large WME source files
wme.txt

# API discovery output files (keep the scripts, not the huge outputs)
wme-api-discovery.json
wme-api-discovery.txt

# Local Claude pointer (machine-specific paths — keep out of the public repo)
CLAUDE.md

# OS files
.DS_Store
Thumbs.db

# Editor files
.vscode/
.idea/
*.swp
*.swo
*~

# Node modules (if any)
node_modules/

# Local Tampermonkey stub loader (hardcodes a machine-specific file:/// path)
wme-rpp-auto-fixer-loader.user.js*
